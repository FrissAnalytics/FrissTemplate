shinyUI(

  navbarPage(id="main", windowTitle = "Friss network viewer", position = "fixed-top", title = NULL,

   header = FrissHeader,

   tabPanel("panel 1", icon = icon("dashboard"),

      h3("add ui code here!")
      
   )
))
